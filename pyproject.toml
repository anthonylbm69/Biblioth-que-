[tool.black]
line-length = 88
target-version = ["py311"]  # adapte si tu utilises une autre version (e.g., "py310")
skip-string-normalization = false

[tool.isort]
profile = "black"

# Optionnel: ajoute d'autres configs d'outils ici (flake8, ruff, etc.)
